import LoginClient from './LoginClient'

export const metadata = {
  title: 'Login | Phone Auth App',
  description: 'Login or register using your phone number and OTP',
}

export default function LoginPage() {
  return <LoginClient />
}
